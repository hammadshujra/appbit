import Head from 'next/head';
import {useCallback,useEffect,useState} from 'react';
const emptyReport={version:'',buildId:'',ok:false,checks:[]};
export default function HealthPage(){const[report,setReport]=useState(null),[error,setError]=useState(''),[loading,setLoading]=useState(true),[checkedAt,setCheckedAt]=useState(null);const refresh=useCallback(async()=>{setLoading(true);try{const response=await fetch(`/api/health/public?t=${Date.now()}`,{cache:'no-store',credentials:'same-origin'});const data=await response.json();setReport({...emptyReport,...data});setCheckedAt(data.checkedAt||new Date().toISOString());setError('')}catch(e){setError(e?.message||'Health check could not be reached.')}finally{setLoading(false)}},[]);useEffect(()=>{let alive=true,timer;const poll=async()=>{await refresh();if(alive)timer=setTimeout(poll,6000)};poll();return()=>{alive=false;clearTimeout(timer)}},[refresh]);const checks=report?.checks||[],healthy=Boolean(report?.ok);return <><Head><title>System health · Appbit</title><meta name="viewport" content="width=device-width,initial-scale=1"/><link rel="icon" href="/favicon.svg" type="image/svg+xml"/><link rel="preconnect" href="https://fonts.googleapis.com"/><link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous"/><link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet"/><link rel="stylesheet" href="/css/app.css?v=251"/></Head><main className="health-page"><section className="health-hero"><div className="health-hero-brand"><img src="/studio-logo.svg?v=251" alt="Appbit"/><div><b>APPBIT</b><span>Deployment health</span></div></div><div><span className="auth-eyebrow">SYSTEM STATUS</span><h1>{healthy?'Your studio is ready.':'Checking your deployment.'}</h1><p>Safe deployment diagnostics for Hostinger. Credentials are never displayed here.</p></div><div className={`health-state ${healthy?'healthy':'waiting'}`}><span></span><b>{healthy?'All core checks passing':'Checking…'}</b></div></section><section className="health-main"><div className="health-heading"><div><h2>System health</h2><p>Runtime, database, schema, and download gateway.</p></div><span className="health-version">V{report?.version||'—'}</span></div>{error?<div className="error-box">{error}</div>:null}<div className="health-checks">{checks.length?checks.map(check=><div className="health-check" key={check.name}><span className={`health-check-icon ${check.status==='pass'?'pass':check.status==='pending'?'pending':'fail'}`}>{check.status==='pass'?'✓':check.status==='pending'?'…':'!'}</span><div><b>{check.name}</b><span>{check.detail}</span></div></div>):<div className="health-empty">Waiting for the first health response…</div>}</div><div className="health-actions"><button className="btn btn-primary" type="button" onClick={refresh}>{loading?'Checking…':'Check again'}</button><a className="btn" href="/login?next=%2F">Open sign in</a>{healthy?<a className="btn" href="/">Open Appbit</a>:null}</div><div className="health-foot"><span>{checkedAt?`Last checked ${new Date(checkedAt).toLocaleTimeString()}`:'Automatic check every 6 seconds'}</span><span>{report?.buildId||'Hostinger native Next.js runtime'}</span></div></section></main></>}
