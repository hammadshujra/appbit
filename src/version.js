'use strict';
// Release identity is bundled into the Next.js server output so Hostinger does
// not need loose metadata files at runtime.
module.exports=Object.freeze({
  version:'2.6',
  buildId:'2.6-app-detail-resolver'
});
